import { SkinBase } from '../mvc/SkinBase';

export class BaseToastItem extends SkinBase {
    $isEnd = false;
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    setMessage(message: string): void {
        //
    }
}
